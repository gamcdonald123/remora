<div id="fleet">
  <div class="rows">
    <% Stack.build(fleet).each do |entry| %>
      <%= render partial: (entry.stack? ? "containers/stack" : "containers/container"),
            locals: { (entry.stack? ? :stack : :container) => entry } %>
    <% end %>
  </div>
</div>
